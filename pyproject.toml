[build-system]
requires = ["hatchling>=1.32.4"]
build-backend = "hatchling.build"

[project]
name = "krossdb"
version = "0.1.0"
description = "Enterprise-grade, multi-database unified interface & repository pattern library for Python"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "KrossDB Maintainers" }]
keywords = [
    "database", "repository-pattern", "unit-of-work", "postgresql", "mysql",
    "sqlite", "mongodb", "qdrant", "vector-database", "async", "sqlalchemy",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]

dependencies = [
    # Data validation / settings
    "pydantic>=2.13,<3.0",
    "pydantic-settings>=2.15.0,<3.0",
    # Relational (async SQLAlchemy)
    "sqlalchemy[asyncio]>=2.0.54,<3.0",
    # Observability
    "structlog>=25.1",
    "opentelemetry-api>=1.44.0",
    "opentelemetry-sdk>=1.44.0",
    # Resilience
    "tenacity>=9.1",
    # CLI
    "typer>=0.27.2",
    "rich>=14.2",
    # Misc
    "anyio>=4.15",
    "pyyaml>=6.0.3",
]

[project.optional-dependencies]
postgres = ["asyncpg>=0.31"]
mysql = ["asyncmy>=0.2.15"]
sqlite = ["aiosqlite>=0.21"]
mongodb = ["motor>=3.7.1", "pymongo>=4.18"]
vector = ["qdrant-client>=1.19"]
cache = ["redis[hiredis]>=8.1.0"]
migrations = ["alembic>=1.16"]
otel-exporters = [
    "opentelemetry-exporter-otlp>=1.44.0",
    "opentelemetry-instrumentation-sqlalchemy>=0.65b0",
]
all = [
    "krossdb[postgres,mysql,sqlite,mongodb,vector,cache,migrations,otel-exporters]",
]
dev = [
    "krossdb[sqlite,mongodb,vector,cache,migrations]",
    "pytest>=8.4",
    "pytest-asyncio>=1.0",
    "pytest-mock>=3.15",
    "pytest-cov>=6.0",
    "testcontainers[postgres,mongodb,qdrant]>=4.13",
    "mypy>=1.15",
    "types-PyYAML>=6.0.12.20260815",
    "ruff>=0.13",
    "faker>=40.38.0",
    "httpx>=0.28.1",
    "pre-commit>=4.0",
]

[project.scripts]
krossdb = "krossdb.cli.main:app"

[project.urls]
Homepage = "https://github.com/ayal408/krossdb"
Repository = "https://github.com/ayal408/krossdb"
Changelog = "https://github.com/ayal408/krossdb/blob/main/CHANGELOG.md"
Issues = "https://github.com/ayal408/krossdb/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/krossdb"]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra --strict-markers --strict-config --cov=krossdb --cov-report=term-missing:skip-covered"
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "unit: fast, isolated tests with mocked adapters",
    "integration: tests that spin up real services via Testcontainers",
    "slow: tests that take a long time to run",
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning:google.*",
    "ignore::ResourceWarning:pymongo.*",
    "ignore:Exception ignored in.*socket\\.socket.*:pytest.PytestUnraisableExceptionWarning",
]

[tool.coverage.run]
source = ["src/krossdb"]
branch = true
omit = ["*/cli/*"]

[tool.coverage.report]
fail_under = 80
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",
]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
mypy_path = "src"
namespace_packages = true
explicit_package_bases = true

[[tool.mypy.overrides]]
module = ["asyncpg", "asyncpg.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["alembic", "alembic.*"]
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "SIM", "RUF"]
ignore = [
    "E501",
    "B008",  # typer.Option(...)/typer.Argument(...) as a default is the documented Typer idiom
    "B027",  # AbstractUnitOfWork._persist_events is an intentional no-op extension hook, not missed abstractness
    "RUF022",  # __all__ is intentionally grouped by topic (with comments), not alphabetized
]

[tool.ruff.lint.per-file-ignores]
"src/krossdb/cli/commands/*.py" = ["ASYNC240"]  # CLI commands read small local config files synchronously by design

[tool.ruff.lint.isort]
known-first-party = ["krossdb"]
